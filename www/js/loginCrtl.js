'use strict';

var loginCrtl = function ($scope, Auth) {
  $scope.facebookLog = function () {
    
  }
}

loginCrtl.$inject = ['$scope', 'Auth'];

angular.module('devfest').controller('loginCrtl', loginCrtl);
